# .env.example - copy to .env and customize for your environment
#
# IMPORTANT:
# - Never commit real secrets.
# - Use a long, random Erlang cookie (32+ chars) and keep it identical across all cluster nodes.
# - Rotate bootstrap admin credentials after initial setup; create least-privilege users per app/team.

# Erlang cookie shared by all nodes in the cluster (KEEP SECRET)
RABBITMQ_ERLANG_COOKIE=please_change_me_to_a_long_random_secret_value

# Bootstrap admin for the first node (rabbitmq1)
RABBITMQ_DEFAULT_USER=admin
RABBITMQ_DEFAULT_PASS=ChangeThisStrongP@ssw0rd!

# Default vhost
RABBITMQ_DEFAULT_VHOST=/
