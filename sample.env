# Copy this file to .env and customize values before running in production
# Erlang cookie must be identical across all cluster nodes and kept secret
RABBITMQ_ERLANG_COOKIE=change_me_use_a_long_random_secret_value

# Bootstrap admin credentials for the first node (rabbitmq1)
RABBITMQ_DEFAULT_USER=admin
RABBITMQ_DEFAULT_PASS=StrongP@ssw0rd_change_me
RABBITMQ_DEFAULT_VHOST=/